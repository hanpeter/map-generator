(function () {
    'use strict';

    var controller = function ($scope) {
        _.extend($scope, {

        });
    };

    controller.$inject = ['$scope'];

    module.exports = controller;
})();
